\section{Comandos útiles}

Aqui hay algunos comando utiles : \cite{latex:companion}

%------ Para insertar y citar una imagen centralizada -----
\subsection{Comando para insertar Imagenes}
\begin{figure}[H]
    \centering
    \includegraphics[width = 0.75\linewidth]{logos/UPV.png}
    \caption{Pie de figura}
    \label{}
\end{figure}

%------- Para insertar y citar una ecuación --------------
\subsection{Comando para insertar y citar una ecuacion}
\begin{equation} \label{eq: exemple}
\rho + \Delta = 42
\end{equation}

La ecuación \ref{eq: exemple} está aqui. \cite{knuth:1984}

% ------- Para escribir variables ----------------------

Para escribir variables en la misma linea, hay que poner el símbolo \$ entre el texto tal que: constante $\rho$

%------- Pour ecrire un code ------------------------------
\newpage
\subsection{Comando para mostrar código}

\begin{minted}{python3}
import numpy as np
    
def incmatrix(genl1,genl2):
    m = len(genl1)
    n = len(genl2)
    M = None #to become the incidence matrix
    VT = np.zeros((n*m,1), int)  #dummy variable
    
    #compute the bitwise xor matrix
    M1 = bitxormatrix(genl1)
    M2 = np.triu(bitxormatrix(genl2),1) 

    for i in range(m-1):
        for j in range(i+1, m):
            [r,c] = np.where(M2 == M1[i,j])
            for k in range(len(r)):
                VT[(i)*n + r[k]] = 1;
                VT[(i)*n + c[k]] = 1;
                VT[(j)*n + r[k]] = 1;
                VT[(j)*n + c[k]] = 1;
                
                if M is None:
                    M = np.copy(VT)
                else:
                    M = np.concatenate((M, VT), 1)
                
                VT = np.zeros((n*m,1), int)
    
    return M
\end{minted}


